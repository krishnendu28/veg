services:
  - type: web
    name: cbk-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm ci
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: MONGO_URI
        sync: false
      - key: ALLOWED_ORIGINS
        value: "http://localhost:5173,http://localhost:5174,http://localhost:8081,http://127.0.0.1:8081,http://localhost:19006,http://127.0.0.1:19006"
      - key: ALLOW_RENDER_PREVIEWS
        value: "true"
      - key: ADMIN_API_KEYS
        sync: false

  - type: cron
    name: cbk-backend-keepwarm
    env: node
    plan: free
    rootDir: backend
    schedule: "*/10 * * * *"
    buildCommand: npm ci
    startCommand: >-
      node -e "const url = process.env.BACKEND_HEALTHCHECK_URL;
      if (!url) { console.error('BACKEND_HEALTHCHECK_URL is missing'); process.exit(1); }
      fetch(url, { headers: { 'user-agent': 'cbk-render-keepalive' } })
        .then((res) => { if (!res.ok) throw new Error('Healthcheck failed: ' + res.status); console.log('Keepalive success'); })
        .catch((err) => { console.error(err.message); process.exit(1); });"
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: BACKEND_HEALTHCHECK_URL
        sync: false
